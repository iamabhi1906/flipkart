services:
  frontend:
    build:
      context: .
    container_name: flipkart-frontend
    env_file:
      - ./.env.production.local
    ports:
      - "3000:3000"
    restart: always
    networks:
      - app-network

networks:
  app-network:
    external: true
